import Base from "./base";
import { IOrder } from "./response/order";
import OrderPayload from "./payload/orderPayload";
import PatchOrderPayload from "./payload/order/patchOrderPayload";
export default class Order extends Base {
    create(market: string, orderPayload: OrderPayload): Promise<IOrder>;
    edit(market: string, orderId: string, patchOrderPayload: PatchOrderPayload): Promise<IOrder>;
    retrieve(market: string, orderId: string): Promise<IOrder>;
    cancel(market: string, orderId: string): Promise<boolean>;
    addPriorityFee(market: string, orderId: string, fee: string): Promise<boolean>;
}
