"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class PatchOrderPayload {
    constructor(builder) {
        var _a;
        const { stops } = builder;
        const stopsLength = (_a = stops === null || stops === void 0 ? void 0 : stops.length) !== null && _a !== void 0 ? _a : 0;
        if (stopsLength > 17 || stopsLength < 2) {
            throw new Error("Stops must be between 2 and 17");
        }
        if (!(stops === null || stops === void 0 ? void 0 : stops.every((stop) => stop.address))) {
            throw new Error("Address cannot be empty");
        }
        this.stops = stops;
    }
}
exports.default = PatchOrderPayload;
