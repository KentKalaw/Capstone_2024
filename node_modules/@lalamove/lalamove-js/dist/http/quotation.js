"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const base_1 = __importDefault(require("./base"));
class QuotationHTTPClient extends base_1.default {
    static toIQuotation(resolve) {
        return (d) => {
            var _a;
            const quotation = d;
            quotation.id = quotation.quotationId;
            delete quotation.quotationId;
            (_a = quotation.stops) === null || _a === void 0 ? void 0 : _a.forEach((stopData) => {
                const stop = stopData;
                stop.id = stop.stopId;
                delete stop.stopId;
            });
            resolve(quotation);
        };
    }
    create(market, path, body) {
        return new Promise((resolve, reject) => {
            const response = this.makeCall(market, path, body, "POST");
            response
                .then(QuotationHTTPClient.toIQuotation(resolve))
                .catch(QuotationHTTPClient.errorHandler(reject));
        });
    }
    get(market, path) {
        return new Promise((resolve, reject) => {
            const response = this.makeCall(market, path);
            response
                .then(QuotationHTTPClient.toIQuotation(resolve))
                .catch(QuotationHTTPClient.errorHandler(reject));
        });
    }
}
exports.default = QuotationHTTPClient;
