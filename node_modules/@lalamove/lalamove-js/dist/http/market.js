"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const base_1 = __importDefault(require("./base"));
class MarketHTTPClient extends base_1.default {
    get(market, path) {
        return new Promise((resolve, reject) => {
            const response = this.makeCall(market, path);
            response
                .then((d) => {
                const cities = d === null || d === void 0 ? void 0 : d.map((city) => {
                    const temp = {
                        id: city.locode,
                    };
                    Object.keys(city).forEach((key) => {
                        if (key !== "locode") {
                            Object.assign(temp, { [key]: city[key] });
                        }
                    });
                    return temp;
                });
                const iMarket = {
                    id: market,
                    cities,
                };
                resolve(iMarket);
            })
                .catch(MarketHTTPClient.errorHandler(reject));
        });
    }
}
exports.default = MarketHTTPClient;
