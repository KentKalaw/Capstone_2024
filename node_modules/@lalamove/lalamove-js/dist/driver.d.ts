import Base from "./base";
import { IDriver } from "./response/driver";
export default class Driver extends Base {
    retrieve(market: string, id: string, orderId: string): Promise<IDriver>;
    cancel(market: string, id: string, orderId: string, reason: string): Promise<boolean>;
}
